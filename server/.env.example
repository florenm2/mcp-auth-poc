# Server Configuration
PORT=3000
HOST=localhost

# Server Mode: http (OAuth endpoints) or mcp (stdio MCP server)
SERVER_MODE=http

# OAuth Configuration
OAUTH_ISSUER=http://localhost:3000
JWT_SECRET=your-secret-key-change-this-in-production

# MCP Configuration
MCP_SERVER_NAME=mcp-auth-demo
MCP_SERVER_VERSION=1.0.0

# Client Registration
# Allow dynamic client registration
ALLOW_DCR=true
